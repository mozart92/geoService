import { ActiveRouteTransformeDirective } from './active-route-transforme.directive';

describe('ActiveRouteTransformeDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveRouteTransformeDirective();
    expect(directive).toBeTruthy();
  });
});
